{
  "name": "pythonmentor",
  "displayName": "PythonMentor - Socratic Python Tutor",
  "description": "Personalized Python learning assistant with Socratic teaching method",
  "version": "0.1.6",
  "publisher": "Eddie-58-cgw",
  "author": "Eddie",
  "license": "MIT",
  "icon": "images/icon.png",
  "keywords": ["python", "education", "tutor", "socratic", "learning", "AI"],
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Education", "Other"],
  "activationEvents": [
    "onView:python-mentor.chat",
    "onCommand:python-mentor.openChat",
    "onCommand:python-mentor.diagnoseError",
    "onCommand:python-mentor.startBackend",
    "onCommand:python-mentor.stopBackend",
    "onCommand:python-mentor.configureApiKeys"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "python-mentor",
          "title": "PythonMentor",
          "icon": "media/icon.png"
        }
      ]
    },
    "views": {
      "python-mentor": [
        {
          "type": "webview",
          "id": "python-mentor.chat",
          "name": "Chat"
        }
      ]
    },
    "commands": [
      {
        "command": "python-mentor.openChat",
        "title": "PythonMentor: Open Chat",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "python-mentor.diagnoseError",
        "title": "PythonMentor: Diagnose Error",
        "icon": "$(warning)"
      },
      {
        "command": "python-mentor.startBackend",
        "title": "PythonMentor: Start Backend Server",
        "icon": "$(play)"
      },
      {
        "command": "python-mentor.stopBackend",
        "title": "PythonMentor: Stop Backend Server",
        "icon": "$(stop)"
      },
      {
        "command": "python-mentor.configureApiKeys",
        "title": "PythonMentor: Configure API Keys",
        "icon": "$(settings-gear)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "python-mentor.diagnoseError",
          "when": "editorHasSelection",
          "group": "python-mentor"
        }
      ]
    },
    "configuration": {
      "title": "PythonMentor",
      "properties": {
        "python-mentor.backendUrl": {
          "type": "string",
          "default": "http://localhost:8000",
          "description": "Python 后端服务地址"
        },
        "python-mentor.studentId": {
          "type": "string",
          "default": "default",
          "description": "学生 ID，用于区分不同学生的学习档案"
        },
        "python-mentor.pythonPath": {
          "type": "string",
          "default": "python",
          "description": "Python 解释器路径（留空则自动检测）"
        },
        "python-mentor.llmBackend": {
          "type": "string",
          "enum": ["ollama", "claude", "openai"],
          "default": "ollama",
          "description": "LLM 后端选择"
        },
        "python-mentor.claudeApiKey": {
          "type": "string",
          "default": "",
          "description": "Claude API Key (or Anthropic-compatible service key)"
        },
        "python-mentor.claudeBaseUrl": {
          "type": "string",
          "default": "",
          "description": "Anthropic-compatible API base URL (leave empty for official Anthropic)"
        },
        "python-mentor.claudeModel": {
          "type": "string",
          "default": "claude-sonnet-4-20250514",
          "description": "Claude model name"
        },
        "python-mentor.openaiApiKey": {
          "type": "string",
          "default": "",
          "description": "OpenAI API Key (or OpenAI-compatible service key)"
        },
        "python-mentor.openaiBaseUrl": {
          "type": "string",
          "default": "",
          "description": "OpenAI-compatible API base URL (e.g. DeepSeek, Xiaomi, etc. Leave empty for official OpenAI)"
        },
        "python-mentor.openaiModel": {
          "type": "string",
          "default": "gpt-4o",
          "description": "OpenAI model name (e.g. gpt-4o, deepseek-chat, etc.)"
        },
        "python-mentor.ollamaUrl": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Ollama 服务地址"
        },
        "python-mentor.ollamaModel": {
          "type": "string",
          "default": "qwen2.5:14b",
          "description": "Ollama 模型名"
        },
        "python-mentor.embeddingApiKey": {
          "type": "string",
          "default": "",
          "description": "Embedding API Key (required for RAG, supports DashScope/OpenAI)"
        },
        "python-mentor.embeddingModel": {
          "type": "string",
          "default": "text-embedding-v4",
          "description": "Embedding model name"
        },
        "python-mentor.embeddingApiUrl": {
          "type": "string",
          "default": "https://dashscope.aliyuncs.com/compatible-mode/v1",
          "description": "Embedding API base URL"
        },
        "python-mentor.maxTokens": {
          "type": "integer",
          "default": 2048,
          "minimum": 256,
          "maximum": 16384,
          "description": "LLM 单次回复最大 token 数（输出长度上限）"
        },
        "python-mentor.temperature": {
          "type": "number",
          "default": 0.7,
          "minimum": 0.0,
          "maximum": 2.0,
          "description": "LLM 温度参数：越低越确定（0.0），越高越随机（2.0）"
        },
        "python-mentor.topP": {
          "type": "number",
          "default": 1.0,
          "minimum": 0.0,
          "maximum": 1.0,
          "description": "Top-P 采样参数：控制词汇多样性，1.0 表示不启用"
        },
        "python-mentor.contextWindow": {
          "type": "integer",
          "default": 40,
          "minimum": 5,
          "maximum": 100,
          "description": "发送给 LLM 的最近对话轮数（影响输入 token 消耗）"
        }
      }
    }
  },
  "scripts": {
    "build": "webpack --mode production",
    "build:dev": "webpack --mode development",
    "watch": "webpack --watch --mode development",
    "copy-deps": "node scripts/copy-backend.js",
    "clean-deps": "node scripts/clean-backend.js",
    "package": "npm run build && npm run copy-deps && vsce package --allow-missing-repository && npm run clean-deps",
    "publish": "npm run build && npm run copy-deps && vsce publish --allow-missing-repository && npm run clean-deps"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.0",
    "ts-loader": "^9.5.0",
    "@vscode/vsce": "^2.0.0"
  }
}
